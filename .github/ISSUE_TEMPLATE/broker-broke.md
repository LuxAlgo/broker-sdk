---
name: Broker adapter broken
about: A broker changed something and an adapter stopped normalizing correctly
labels: adapter
---

**Broker:**

**What happens:** (error message, or which fields come back wrong/missing)

**Raw payload (REDACT all keys, tokens, account numbers, and balances you don't want public):**

```json
```

**What the normalized output should be:**

The most useful report is a failing conformance vector — see CONTRIBUTING.md. If you can, open a PR with the vector and the fix together.
